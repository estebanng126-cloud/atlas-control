# Copiar a `.env` en este mismo directorio (`packages/db/.env`).
# La ruta es relativa al directorio `prisma/` (donde está `schema.prisma`).
DATABASE_URL="file:../dev.db"

# Opcional: mismo archivo lo lee la API al arrancar (vía @atlas/db) si quieres fijar puerto sin export manual.
# PORT=3001
